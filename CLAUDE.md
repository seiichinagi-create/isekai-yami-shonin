# CLAUDE.md

ローカルの Claude Code がこのリポジトリで最初に読むファイルです。

## このリポジトリは何か

ブラウザゲーム **「異世界闇商人」**。迷宮都市で商店を経営する、オリジナル設定のゲームです。
固有名詞や世界観を既存作品から借りていません(経緯は `docs/handoff.md` の「オリジナルへの転換」を参照)。

> 世界が滅びない程度に戦争を支え、世界が救われない程度に供給を絞る。
> あなたは迷宮都市の商人であり、この均衡そのものを商材にしている。

**目的はクリアしないこと。** 前線が崩れて世界が滅んでも負け、魔王が討たれて需要が消えても負けです。

## 運用 — NAS が正、GitHub はミラー(オーナーの普段どおり)

- **正は NAS の git**(リモート名 `nas`・`main` の追跡先。URL はオーナーの他の repo と同じ並びで、ここには書かない)
- **GitHub `seiichinagi-create/isekai-yami-shonin`(PRIVATE)はミラー**(リモート名 `origin`)。職場PCは NAS に届かないので GitHub 経由
- **作業実体は `C:\dev\isekai-yami-shonin`**。開発はローカルの Claude Code でやる
- 節目ごとに `git push nas main` と `git push origin main` の両方

経緯: 2026-09-19 の起草時は「GitHub が唯一の正・NAS に置かない」という例外だった(企画を claude.ai のプロジェクトスレッドで起こしたため)。
同日夜、オーナーが**撤回**した。クラウド側のプロジェクト環境では GitHub を満足に扱えず、当初の見極めが済んだので普段の構成へ戻す、という裁定。
**クラウド側のスレッドで何か書いた場合は、ローカルへ取り込んで NAS へ入れ直すこと**(GitHub へ直接 push された変更があれば、先に `git fetch origin` で確かめる)。

## 構成

| パス | 中身 |
|---|---|
| `prototype/ledger.html` | 第一章の試作「迷宮都市の帳場」。依存なしの単一 HTML |
| `design/game-concept-v1.md` | 企画本体。世界設定から終盤まで全14節 |
| `design/research/wiz1-boltac-worldbuilding.md` | 売買まわりの調査。**設定ではなく仕組みの参考資料** |
| `docs/handoff.md` | ここまでの経緯、決まったこと、未決のこと |

まず `docs/handoff.md` を読むと、どこまで決まっていて次に何をするかが分かります。

## 試作を動かす

`prototype/ledger.html` をブラウザで開くだけ。ビルドもサーバーも要りません。
セーブは localStorage の `ledger.ch1.v1`、1スロット。

### 試作をいじるときの注意

この試作はもともと claude.ai の Artifact として作られたもので、**ホストが `<head>` を補っていました**。
リポジトリ版はそれを単体で開ける文書に直してあります。**作り直したり Artifact 版から取り直したりするときは、次の2つが要ります。どちらも実際に壊れます。**

1. **`<meta charset="utf-8">`** — 無いと `file://` で開いたとき日本語が文字化けする
2. **`[hidden]{display:none!important}`** — `.priceset` が `display:flex` を持つので、作者スタイルが UA の `[hidden]` 規則に勝ってしまう。値付け欄が最初から開きっぱなしになる

Artifact 専用の `window.claude.hot` フックは外してあります。

## 言語

オーナーは日本語で書きます。**日本語で応答してください。** コード内のコメントも日本語です。

## 次にやること

第一章の試作を実際に遊んで、**どこが退屈かを見つけること**。そこで第二章の設計が決まります。
企画書の第10節にある通り、MVP が退屈なら設定をいくら足しても面白くなりません。
